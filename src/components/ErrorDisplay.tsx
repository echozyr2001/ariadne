import React from "react";
import { Box } from "ink";
import type { ErrorDisplayProps } from "./types.js";

const ErrorDisplay: React.FC<ErrorDisplayProps> = ({
  error,
  type = "general",
}) => {
  return <Box></Box>;
};

export default ErrorDisplay;
