{
  "name": "ariadne-workspace",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "bun run packages/ariadne/index.ts",
    "build": "bun build ./packages/ariadne/index.ts --compile --minify --sourcemap=external --outfile dist/ari",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
